# technical-test-lobster

## Setup

To set up this project, you'll need to install the Diesel CLI for PostgreSQL support. Run the following command:

```bash
cargo install diesel_cli --no-default-features --features postgres